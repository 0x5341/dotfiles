{{- /* chezmoi:modify-template */ -}}
{{- .chezmoi.stdin -}}

{{- $flag := `# chezmoi:add-flag` -}}

{{- if not (contains $flag .chezmoi.stdin) -}}
{{ $flag }}

# mise activate
eval "$(~/.local/bin/mise activate bash)"

{{ end -}}
